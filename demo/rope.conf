# rope.conf — ROPE runtime configuration (demo)
#
# Relative paths are resolved relative to this config file's directory.

# ---------------------------------------------------------------------------
[paths]
# ---------------------------------------------------------------------------

exported_dir = ../data/models/tiegcm-aurora-v1

# Explicit driver file — bypasses online cache.
# Remove once CelesTrak→ROPE conversion is implemented.
driver_path = ../data/drivers/sw_celestrack_1957.swbin


# ---------------------------------------------------------------------------
[driver_cache]
# ---------------------------------------------------------------------------

max_age_hours = 24


# ---------------------------------------------------------------------------
[threads]
# ---------------------------------------------------------------------------

intra_threads_base    = 1
intra_threads_meta    = 0
intra_threads_decoder = 0


# ---------------------------------------------------------------------------
[decoder]
# ---------------------------------------------------------------------------

device = cpu


# ---------------------------------------------------------------------------
[forecast]
# ---------------------------------------------------------------------------

# Uncertainty calculation is disabled for the demo — it is slow and the
# demo only needs density estimates, not the full UT uncertainty envelope.
compute_uncertainty = false

# Bounds peak memory during decode. See config/rope.conf.in.
decode_chunk_hours = 72
